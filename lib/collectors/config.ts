/**
 * 포털 한 곳을 기다리는 시간.
 *
 * 로컬에서는 5초로 충분했지만 Cloudflare 엣지에 올리니 국내 포털까지 왕복이
 * 느려져 매 요청마다 가장 느린 포털 하나가 타임아웃으로 떨어졌다(실측 5~8초,
 * 실패하는 포털이 요청마다 바뀜). 세 수집기를 병렬로 부르므로 이 값을 올려도
 * 전체 응답은 가장 느린 한 곳만큼만 늦어진다.
 */
export const COLLECT_TIMEOUT_MS = 12_000;
